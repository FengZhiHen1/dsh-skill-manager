# DSR-022：操作级审计台账与挂载归属标记

> 状态：**A 案（第 1–9 条）已实现并过闸（2026-09-09，见文末「实现状态」，其中含对第 4/5/9 条的实现期修订）；B 案（第 10–11 条）未开工**（前置未验）。
> 立项经用户认可：1、2 两案照采，第 3 案细化为「操作级、只留文件、Agent 自查」。
> 本记录只写设计；实现按「实现顺序」节推进，另起提交。

## 上下文

2026-09-09 事故：用户报「`E:\Project\Skills` 中 `yao-meta-skill` 的挂载链接不见了」，本会话穷举取证**无法归因**。事后盘点出四条独立原因，全部指向设计缺口而非检索不力：

1. **插件自述无台账**——`src/core/mount/reconcile.js:29` 注释原文：「摘除 = 孤儿清扫（同一归属判据，**无第二张台账**）」。
2. **对账结果不落盘**——`reconcile()` 的 `results`（含 `name`/`target`/`action`/`reason`）返回调用方 → UI toast 一闪即弃。`core/**` 里全部持久化写入点只有 `store.putSkill` / `store.putCheck` / `deleteCheck`（skills 表 + check_cache），**没有任何一张表记链接操作**。
3. **日志只有错误路径**——全插件仅 4 条日志调用（`adapter/index.js:22,38,50,90`，皆 `logger.warn`）；**对账成功时零输出**。`logger` 虽注入 `buildApi`（`adapter/index.js:75`），挂载路径一次都没用它。
4. **环境侧退路全断**——目标仓库 `.gitignore:9` 忽略整个 `.dsh/`（`git check-ignore -v` 实证）→ VCS 无痕；junction 删除不进回收站；NTFS 只留父目录一次 `LastWriteTime`（`E:\Project\Skills\.dsh\skills` = 09-09 **09:01:32.547**，比 `homes\test\settings.yaml` 写入 09:01:32.**315** 晚 **232ms**，与插件 200ms 防抖同量级——**这是时间签名，属推断，不是证据**），且不带文件名；Windows junction 的 reparse 数据放不下自定义来源标记。

根因陈述：归属判据是 **realpath 前缀**（`core/mount/inspect.js:36-41`、`materialize.js:65,106`），链接一旦被删，**「曾被 A 实例建过」与「从未存在」在系统里不可区分**。

同一次排查还坐实第二件事：工作区注册表按 HOME 存放（`storages/workspace.json`），试验 HOME 从生产继承来 **13 条真实项目**（含 `E:\Project\Skills`、`E:\Project\DSH_Plugins`）——配置里残留一条 `scope: project` 挂载，对账就会直接往真实项目建/摘链接；两个 HOME 的实例共用同一配置目录时**互摘链**（已记入 `technical-details/挂载与同步.md`）。本 DSR 的归属标记就是封这条通道的。

## 真实方向与评价

**归属判据三案**：
- 甲（现状）realpath 前缀：零文件成本；但跨实例互摘、事后不可归因——今天的事故通道。
- 乙 链接名编码 owner（前缀/后缀）：**不可行**——DSH 按目录名识别 skill（`knowledge/v0.1.2-rc.1/agent/23-skill.md`），改名即改语义。
- 丙 每个挂载根一份**本 HOME 的托管清单**（manifest）：归属精确、可归因；代价是多一个受管文件、需要一次性认领（adopt）迁移、且**必须落在 DSH skill 根扫描能容忍的位置**（未验证，见「实现顺序」B 的前置）。

**审计埋点两案**：
- 在每个业务调用点手写审计调用：分散，新增变更点必漏（今天就是因为变更点分散在 5 个模块）。
- **收敛到 fs 咽喉层**：变更只发生在少数几个原语调用上（实测清单见下），一处埋点覆盖全部变更面，且天然记到失败分支。选此。

**读取面两案**：
- 提供 `audit` RPC 方法 / 页面「操作记录」面板：查询方便，但增加契约面、UI 成本与版本兼容负担。
- **只留 JSONL 文件，Agent 按需自查**（用户 2026-09-09 决定）：零契约成本。其代价必须被正视——**文件格式即接口**，因此格式约束写进本 DSR（见「最终决定」第 7 条），违反即视为破坏接口。

## 最终决定（2026-09-09）

1. **台账位置**：`$DSH_HOME/skill-manager/audit.jsonl`，与 `library`/`backups` 同由 `ctx.dshHomePath('skill-manager', …)` 派生（`adapter/index.js:60` 同款）。它**不参与对账**、不被「出库」删除、不进任何期望集。
2. **粒度 = 单操作**：一次链接新建 / 摘除 / 重建、一次目录换装或覆盖下载、一次 exclude 托管块写入 = 各一条记录。**批次摘要不能替代单条**（这是本 DSR 的立项理由）。
3. **两段式**：副作用之前落 `pending`，之后补终态（同 `opId`）。进程中途被杀 → 留下**无终态的 `pending`**，它本身就是「动手了但没记完」的证据。
4. **覆盖面 = fs 咽喉点清单**（实测枚举，逐点埋；新增变更点必须同步登记进本清单。⚠ 行号标的是**变更发生在哪里**，不是「每行一条记录」——记录粒度按语义 op 聚合，实现期确立的五条聚合规则见文末「实现状态」R1）：
   - `core/mount/materialize.js:23,26`（`removeLink`：删链接 / 递归删）
   - `core/mount/materialize.js:54`（`mkdir` 父目录）、`:89`（`symlink` 建 junction）
   - `core/mount/reconcile.js:93`（`.git/info/exclude` 托管块写入）
   - `core/inbound/zipball.js:115,116,150,153`（下载落盘、stage 目录、`cp`）
   - `core/inbound/backups.js:45,54,71,178`（备份目录建立/回滚、源目录删除）
   - `core/base/fsys.js:86-96`（原子写/删受管文件）、`:208-242`（`atomicSwapDir` 换装与回滚）
5. **每条记录的字段**（单行完整 JSON，键名冻结）：`seq`（进程内单调递增，防时钟回拨与并发乱序）、`ts`（ISO-8601 带时区）、`opId`（pending 与终态配对）、`phase`（`pending`/`done`/`failed`）、`actor`（`{entry: rpc|watch|tool|boot|client, method}`）、`home`、`profile`、`pid`、`procStarted`、`skill`、`path`（被改对象绝对路径）、`target`（链接目标 realpath）、`srcRoot`、`reason`（判据/触发原因，如「孤儿链接（归属本插件且不在期望集）」）、`configGen`（本次读到的配置代次，与 `shared.bundleGen` 同源）、`code`/`error`、`durationMs`。
6. **不记只读**：`overview`/`check`/`diff` 等不改 fs 的方法零记录。
7. **格式即接口**：一行一条完整 JSON；绝对路径原样（不缩写、不正反斜杠混用——统一 `path.resolve` 产物）；**不压缩、不加密、不分片到需要工具才能读的形态**；轮转后仍是 `*.jsonl`，保证 `Select-String`/`rg` 单命令可答「这条链接被谁在什么时候动过」。
8. **noop 不逐条**：每次操作批次结束落一条 `phase:'summary'`，带 `evaluated`（期望条目数）/`orphans`（孤儿数）/`changed`（终态非 noop 数）。用途是区分「执行了但没变化」与「根本没执行」。
9. **双上限截断**：> **5000 行** 或 > **90 天**，先到先生效；实现为「写临时文件 + `rename` 原子替换」，检查时机 = 批次结束后（不是每条写入都重排）。截断本身也要落一条 `phase:'rotate'` 记录（记删掉了多少行、时间范围），否则台账会出现无法解释的空洞。
10. **归属标记（manifest）**：每个挂载根一份受管清单（拟 `.dsh/skills/.managed.json`，最终位置取决于第 11 条验证），登记**本 HOME 建过哪些链接**（名字 + 目标 realpath + 建立时间 + opId）。`findOrphanLinks` 改为**只摘自己清单里登记的**；不再凭 realpath 前缀推断归属。
11. **一次性认领（adopt）**：升级后首趟对账，把「realpath 前缀判为自有、但不在 manifest」的既有链接**认领进 manifest 并落 `phase:'adopt'` 记录**，不直接摘除——避免升级瞬间把别的实例的链接当成非自有而放任，也避免把生产链接误摘。adopt 期 realpath 前缀判据保留为兼容通道。
12. **实现顺序（相对早先口头顺序的调整，理由在此）**：A 台账（1–9）→ B 归属标记（10–11）。原话是「2 优先于 1」，现调整：A 纯追加写、**不碰任何判据**，零回归风险，且 B 的 adopt 争议需要 A 的记录来核对；B 依赖一个未验证前提——**DSH 把 `.dsh/skills` 当 skill 根扫描时能否容忍非目录条目**，必须先按 `agent/23-skill.md` 核对并在实测场验证，不通过就换位置（如 `.dsh/skills-managed.json`，落在 skill 根之外）。

## 直接后果

- 每次对账多 N 次追加写（N = 实际变更数，稳态收敛时 N=0 只多一条 summary）；台账自身写失败**必须进 `results` 与健康面**，不得静默——否则又是一次「没记下来」。
- 归属判据从「推断」变成「登记」：跨实例互摘链被物理封死（A 实例的清单里没有 B 实例的链接 → 摘不到）。代价是 manifest 成为新的受管状态，需要与链接现场自愈对账（现场有链接、清单无登记 → adopt；清单有登记、现场无链接 → 记 `missing` 并保留登记，等重建）。
- 事故可追责：下次同类问题，一条 `rg 'yao-meta-skill' audit.jsonl` 就能给出时间、入口、pid、HOME 与原因。
- 与 DSR-017（junction-only 物化与状态面极简）有张力：本 DSR 明确**新增**两份状态（audit.jsonl、manifest），是对「状态面极简」的一次有意让步——理由是 DSR-017 省的是**可推导**的缓存，而归属与追责记录**不可推导**，丢了就永久丢了。

## 验证计划（成功判据，实现时逐条落为回归闸）

1. 单测（tmp 根）：建链 → 台账出现 `pending` + `done` 两行、同 `opId`、字段齐全；断链 → 出现 `reason` 含「孤儿」的摘除记录。
2. 崩溃路径：在 `symlink` 前注入抛错 → 台账留下**无终态 `pending`**，且 `results` 里有对应 error。
3. 只读不变量：连续调 `overview`/`check` 二十次 → 台账行数零增长。
4. noop 语义：稳态收敛下触发一次 `sync` → 只多一条 `summary`，`changed: 0`。
5. 双上限：灌 5001 行 → 触发截断并落 `rotate` 记录；行数回到 ≤5000 且保留的是最近的。
6. 跨实例隔离（B）：两个 tmp HOME 共用同一配置目录，A 实例对账后，B 实例的链接**必须原样存在**（今天的事故对照组，删掉 manifest 判据即红）。
7. adopt（B）：预置「前缀判为自有、清单无登记」的链接 → 首趟对账不摘除、落 `adopt` 记录、写进清单。
8. 现场冒烟（按红线由用户在 test 实例执行）：改一次分组挂载，把 `audit.jsonl` 新行贴回来核对字段可读性。

## 重访条件

- 台账体积或追加写成为可感知开销（库/目标规模增长）→ 拆分为按挂载根的分区台账，或改为 SQLite；届时仍须保证「单命令可查一个对象的历史」。
- DSH 提供官方插件日志/审计通道（含轮转与检索）→ 本 DSR 第 1–9 条退役，只保留归属标记（10–11）。
- manifest 与链接现场出现无法自愈的分歧（如用户在项目侧手工删链接）→ 重访「登记优先」还是「现场优先」，并考虑在页面上暴露分歧。
- 若 `agent/23-skill.md` 判定 skill 根不容忍非目录条目且替代位置也不干净 → B 案降级为「台账 + 启动时全量 adopt 快照」，接受跨实例互摘风险并把它写进红线（禁止两实例共用配置目录）。


## 实现状态（A 案，2026-09-09）

落地文件：`src/core/base/audit.js`（新，27 键冻结于 `AUDIT_KEYS`，op 枚举 `AUDIT_OPS`）；埋点 `core/mount/materialize.js`、`core/mount/reconcile.js`、`core/base/fsys.js`（新增 `atomicSwapDirAudited` 包壳）、`core/inbound/{acquire,upstream,backups}.js`；上下文经 `core/service.js` 的会话对象（`ctx.audit/actor/configGen`）下发，装配在 `adapter/index.js`；回归闸 `test/audit.test.mjs`（10 例）。

证据：`npm run check` 退出 0（client 产物新鲜 + 语法 + 分层 + `node --test` 130/130 全绿）；分层门禁单跑通过（core 19 文件）。**未做 test 实例实测**（AGENTS.md 禁止 agent 自行起进程），故本 A 案尚未过发布门禁——上 web 前需用户在 test 实例跑一轮并把新台账行贴回核对。

### 实现期修订（覆盖正文相应条目）

- **R1 聚合粒度**（覆盖第 4 条的读法，评审确立）：一次换装 = 一条 `library-swap`，`zipball.js:150/153` 的 `copyTree` 逐文件 mkdir/cp 与 `:115/116` 的下载暂存**不**单独成条（否则一次 update 淹出上百条）；`removeLink` 的 op 边界是**函数边界**（`materialize.js` 内 `rm` 兜底重试两步不分别成条）；备份 op 覆盖 `backups.js:45–52`（含 `_backup_meta.json` 直写——评审指出的原清单唯一实质缺口），其回滚 `rm(:54)` 折进同一条的 `failed`+`result:'rolled-back'`；库内删目录 `:71` 独立成 `library-remove`。
- **R2 不入台账项**（澄清第 4/6 条）：临时目录与 tmp 清理（`zipball.js:108/137`、`fsys.js:102/209/231`）为瞬时噪声，不入；`ctx.storage` 侧写（`putSkill`/`deleteSkill`/`putCheck`/`deleteCheck`）由平台 JSON 持久化，**不双记**（正文第 2 条枚举曾漏 `deleteSkill`，一并更正）；`net.js:16` 的 `git ls-remote` 只解析远端 ref 不改本地 fs，不入。
- **R3 键序**（覆盖第 5 条）：`actor` 拆平为 `entry`/`method`（grep 友好优先于嵌套美观）；新增 `schema`（=1，恒由本层注入）与 `op`（13 值枚举）两键，另为 summary/rotate 加 `evaluated`/`orphans`/`changed`/`dropped`/`kept` 五键，共 27 键；缺省一律写 `null` 而非省略，保证逐行同构。`target` 由 `auditTarget()` 做 realpath 归一，不可解析写 null（评审：8.3 短名与空串会毒化「按 target 反查谁建的链」）。
- **R4 计数口径**（补第 8 条）：`changed` = 真实变更次数（孤儿摘除 + `mounted` + exclude 实际写入）；`mount-dir-create` 只在 `mkdir(recursive)` 真的新建时落**单段**记录（Node 以返回值告知首个新建目录），稳态零噪音。
- **R5 上限实现**（补第 9 条）：截断保留 `maxLines - 1` 行再补 rotate 自证行，使总行数恒 ≤ 上限（否则每次截断反增一行）；截断失败保留原文件并计一次 failure。
- **R6 降级语义**（收口正文「直接后果」与第 3 点取舍）：台账自身写失败**不阻断业务**——首次一条 `logger.warn`、累计计数，批次末往 `results` 追加 `action:'audit-degraded'` 行（不计入 `errors`）。既满足「不得静默」，又不让磁盘满/权限受限把正常挂载判成失败。
- **R7 已知未做**：`home` 由台账路径反推、未做 realpath 归一（Windows 临时目录下可见 8.3 短名，生产 HOME 路径无此现象）；`profile` 取自 `--profile` 启动参数（知识库确认平台只承诺 `ctx.dshHomePath`，无 profile 可读面），非该形式启动时为 null。
- **附带发现（非本 DSR 范围）**：`mount/materialize.js` 的 `detachLink` 与 `base/fsys.js` 的 `writeJson` 在 src/ 内**零生产调用方**（前者疑被 `backups.remove` 的自有摘链环取代，后者仅 `test/dir.test.mjs` 用到，DSR-015 已记为孤儿导出；`writeJson` 是 B 案 manifest 的天然落点）。二者本次只是补上审计上下文，未清理——清理与否另案决定。

### 验收对照（正文「验证计划」8 条）

| 计划条 | 状态 |
| --- | --- |
| 1 建链两段 + 字段齐 | ✅ `audit.test.mjs` 例 1（含逐行 `Object.keys` 与 `AUDIT_KEYS` 全等） |
| 2 失败/中断留痕 | ✅ 例 7（`failed` 携 code）+ 例 8（`begin` 后不结算 → 无终态 pending）。**手段说明**：symlink 失败在 tmp 夹具内无法自然注入，故这两例走 audit 单元面而非 fs 注入——不为了造错而 stub fs 原语 |
| 3 只读零增长 | ✅ 例 3（`overview`+`warm` 各 20 次，行数不变） |
| 4 noop 只落 summary | ✅ 例 4（第二次 sync 仅 +1 行，`changed:0`、`evaluated:2`） |
| 5 双上限截断 | ✅ 例 5（行数上限）+ 例 6（换未来时钟判超龄，等价重启） |
| 6 跨实例隔离（B） | ⬜ 未做——依赖 manifest 判据 |
| 7 adopt（B） | ⬜ 未做 |
| 8 现场冒烟（test 实例，用户） | ⬜ 未做——agent 不得自行起进程 |

此外新增一例不在正文计划内：**台账写失败降级**（把台账父目录占成普通文件 → 业务照常完成、`results` 出现 `audit-degraded`、`errors` 为空）——它是 R6 的锁。

## 已知不可回收（本 DSR 不解决）

- 2026-09-09 这次「`yao-meta-skill` 链接被谁摘掉」**永久不可归因**：台账只能从上线后开始记。同因，也无法区分它死于 09-06 用户自己那次「已摘除全部 6 处项目级 junction 分发链接」的删除操作，还是死于当日的试验实例。
- 该链接的恢复不依赖本 DSR：需先让内容对生产实例可见（在生产实例重新 `add`，内容进 `$DSH_HOME/skill-manager/library`），「Skill 设计」组的两处工作区挂载会自动重建链接。
